// import { createContext, useContext} from "react";

// export const contentProvider = createContext(null)

// export const usePostContext = useContext(contentProvider);



// 18th October
import { createContext } from "react";

 const contextProvider = createContext()

export {
    contextProvider
}